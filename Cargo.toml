[package]
name = "smt-strings"
version = "0.1.0"
edition = "2021"

[dependencies]
dot = {version = "0.1.4", optional = true}
itertools = "0.14.0"
quickcheck = "1.0.3"
smallvec = "1.14.0"


[dev-dependencies]
quickcheck_macros = "1"
rand = "0.9.0"


[features]
default = ["automata", "sampling"]
automata = ["dep:dot"]
sampling = []
